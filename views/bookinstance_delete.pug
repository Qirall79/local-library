extends layout 

block content

    h1= title 

    p #[strong Are you sure you want to delete this book instance ?]
    form(action="", method="post")
        input(type="hidden", name="instanceid", value=bookinstance._id)
        input(type="submit", value="Delete") 